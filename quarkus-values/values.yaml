build:
  enabled: false

deploy:
  route:
    tls:
      enabled: false

image:
  name: quay.io/redhatworkshops/gitops-helm-quarkus
  #name: image-registry.openshift-image-registry.svc:5000/openshift-gitops/multiple-sources
